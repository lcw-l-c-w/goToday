<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.gotoday.contentLike.ContentLikeMapper">

    <select id="checkHeartExists" resultType="int">
        SELECT COUNT(*)
        FROM content_like
        WHERE user_id = #{user_id} AND content_id = #{content_id}
    </select>
    
    <insert id="updateHeart">
        INSERT INTO content_like (user_id, content_id)
        VALUES (#{user_id}, #{content_id})
    </insert>
    
    <delete id="deleteHeart">
        DELETE FROM content_like
        WHERE user_id = #{user_id} AND content_id = #{content_id}
    </delete>
    
    <select id="getHeart" resultType="int">
        SELECT IFNULL(COUNT(*), 0)
        FROM content_like
        WHERE content_id = #{content_id}
    </select>

</mapper>